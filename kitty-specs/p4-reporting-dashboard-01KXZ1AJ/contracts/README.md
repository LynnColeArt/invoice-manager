# P4 Draft Contracts

`normalized-reporting-snapshot-v1.schema.json` defines the P4-owned planning seam
for synthetic Client, Project, Invoice, PaymentAllocation, and DueWork snapshots
inside the P0 event envelope. `p4-contract-manifest.json` records lifecycle and
P0 dependency. P7 later maps real Frozen producer events into this contract.

