# P3 Draft Contracts

`backup-manifest-v1.schema.json` defines the planning boundary for a complete
offline backup. `p3-contract-manifest.json` records P0 dependency and Draft
lifecycle. Auth/API/deployment fragments and synthetic fixtures are published in
P3-owned paths during implementation and must be Frozen before reliance.

