---
affected_files:
  - path: package-lock.json
    issue: "Committed lock is not byte-identical to the graph generated from the final manifests by pinned npm 11.16.0; it retains one redundant nested js-yaml entry."
blocking_findings: 1
cycle_number: 4
implementation_commit: a2002e9a94709ba4c27080c764fcacb37793734b
mission_slug: p0-contract-spine-01KXYY0J
reviewed_at: '2026-07-20T23:20:40Z'
reviewed_lane_tip: e16166350266f28422b132ca78a9234720da5231
reviewer_agent: 'codex-wp01-advisory-review:node-norris'
verdict: rejected
wp_id: WP01
---

# WP01 Review Cycle 4

Verdict: **CHANGES REQUESTED**

Correction commit: `a2002e9a94709ba4c27080c764fcacb37793734b`.

## Blocking issue

**Issue 1 — the committed lock is not the canonical npm 11.16.0 output**

The override in `package.json` is correctly chain-scoped to
`@hey-api/openapi-ts@0.99.0` →
`@hey-api/json-schema-ref-parser@1.4.4` → `js-yaml@4.3.0`, and the
committed root `js-yaml` registry tuple is correct. However, regenerating
`package-lock.json` from the final `.npmrc` and all three package manifests
in an isolated directory with Node.js `24.18.0` and npm `11.16.0` does not
reproduce the committed bytes:

```text
committed package entries: 611
fresh npm 11.16.0 entries: 610
only committed:
  node_modules/@eslint/eslintrc/node_modules/js-yaml
changed common entries: none
```

The clean npm graph hoists/deduplicates the shared patched `js-yaml@4.3.0`
entry at `node_modules/js-yaml`; the committed lock retains the now-redundant
nested ESLint copy left by the tuple-only correction. This fails T002/T004
and the explicit requirements that the sole lock be tool-generated by the
pinned npm version, contain only the resolved graph, and reproduce from
committed inputs.

Required correction:

1. Regenerate `package-lock.json` from the final manifests with Node.js
   `24.18.0` and npm `11.16.0` rather than editing an existing resolution
   tuple in place. The expected canonical result removes
   `node_modules/@eslint/eslintrc/node_modules/js-yaml` while retaining the
   root `js-yaml@4.3.0` public-registry tuple.
2. Prove a second clean isolated regeneration is byte-identical to the first.
3. Run normal and offline `npm ci`, prove `package.json` and
   `package-lock.json` hashes remain unchanged, and rerun `verify:substrate`,
   `npm ls --all`, full `npm audit --audit-level=high`, and production
   `npm audit --omit=dev --audit-level=high`.

## Passing security and reproducibility evidence retained

- RED reproduction against `a2002e9^`: `npm audit --audit-level=high --json`
  exits 1 with four high findings through
  `@hey-api/openapi-ts@0.99.0` → parser/shared → `js-yaml@4.2.0`, identifying
  `GHSA-52cp-r559-cp3m` and affected range `>=4.0.0 <4.3.0`.
- Correction scope: commit `a2002e9` changes exactly `package.json` and
  `package-lock.json`; every direct dependency version remains unchanged,
  including `@hey-api/openapi-ts` exactly `0.99.0`.
- Exact toolchain: Node.js `24.18.0`, npm `11.16.0`, Zig `0.16.0`, Linux
  x86_64/x64.
- `npm ci --offline`: pass; SHA-256 values for `package.json`
  (`262d99dd...31f5`) and `package-lock.json` (`60e05649...26f`) are identical
  before and after installation.
- `verify:substrate`: pass, including the exact override assertion and
  dependency/peer-tree validation.
- `npm ls --all --json`: exit 0 with `problems: []`; the targeted tree shows
  the parser consuming `js-yaml@4.3.0 overridden` while the direct generator
  stays at `0.99.0`.
- Full and production-only audits: both report `found 0 vulnerabilities`.
- Public npm registry metadata for `js-yaml@4.3.0` matches the committed
  tarball URL and SHA-512 integrity exactly:
  `sha512-1td788aAnnZ5qs7V2QIRl1owjtYpbKt749Y3xauqQgwIIGF/xXWz1wMTEBx5O3LK3lXLVuqXPdPxj2BoFHaW9Q==`.
- No implementation source, workspace manifest, contract artifact, or
  non-WP01-owned file changed in the correction commit.

## Contract round-trip disposition

- `p0-contract-manifest.json`: **PASS** for this correction. It identifies
  `package.json` as a shared touchpoint, while the WP prompt and root metadata
  keep WP01 as the sole npm metadata/lock owner. No other WP-owned source was
  changed.
- `README.md` and the eight JSON/OpenAPI contract schemas: **ORTHOGONAL** to
  this transitive dependency correction. No pinned payload, allowed-value
  set, CLI example, schema fragment, or error message changed.

## Subtask disposition

- T001: **PASS** — exact toolchain and package-manager policy are unchanged.
- T002: **FAIL** — the security override works, but the committed lock is not
  the exact canonical graph produced by npm 11.16.0 from the final manifests.
- T003: **PASS** — the stable command surface is unchanged, and
  `verify:substrate` now asserts the patched override and root lock entry.
- T004: **FAIL** — clean installation is immutable, but clean lock generation
  is not byte-reproducible against the committed lock.

## Anti-pattern checklist

1. Dead code: **N/A** — no public function, class, or module was added.
2. Synthetic-fixture test: **N/A** — no test fixture was added; the correction
   is exercised through npm's actual resolver/install/audit surfaces.
3. Silent empty return: **N/A** — no production code path was introduced.
4. FR coverage: **PASS** — original FR coverage remains intact; the correction
   adds an executable substrate assertion for the exact security override.
5. Frozen surface: **PASS** — the correction changes only the two WP01-owned
   npm metadata paths; no Frozen contract was touched.
6. Locked decision: **FAIL** — the prompt requires a pinned-npm-generated,
   reproducible lock, while the committed lock retains a noncanonical duplicate.
7. Shared-file ownership: **PASS** — WP01 is the declared sole npm metadata
   and lock owner; downstream packages consume these bytes unchanged.
8. Production fragility: **N/A** — no production request, worker, CLI, or
   service code was changed.

The requested charter section fetch for `section:code-review-checklist`
returned no matching section; the generated review prompt, action-scoped
review context, and Node Norris directive set were applied instead.
