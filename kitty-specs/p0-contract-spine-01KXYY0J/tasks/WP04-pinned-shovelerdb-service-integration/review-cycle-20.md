---
affected_files:
  - services/api/build.zig
  - services/api/tests/persistence/shovelerdb_build_discovery.zig
blocking_findings: 0
correction_commits:
  - cc03cc3ab1020ead5b68e97b7db547fe8b2cc558
  - fd3f82919136565ee3587594ff5dc753977e6ce3
cycle_number: 20
implementation_commit: fd3f82919136565ee3587594ff5dc753977e6ce3
mission_slug: p0-contract-spine-01KXYY0J
reviewed_at: '2026-07-21T11:41:44Z'
reviewed_lane_tip: ac4398727e547764b2966817672dbf66acb9d1ce
reviewer_agent: codex-wp04-cycle20-reviewer
verdict: approved
wp_id: WP04
---

# WP04 Review Cycle 20 — APPROVED

Cycle 19's exact canonical route-inventory embed blocker is closed. The
correction maps only the locked embed literal to the canonical generated JSON,
preserves `http={shared}` as the executable Zig capability graph, and leaves
composition, tests, materialization, emitted-executable, run, and fail-closed
behavior intact. No blocking finding remains.

## Correction verification

- Test-first chronology — PASS. `cc03cc3` is a test-only direct child of the
  pre-correction lane tip. `fd3f829` is its direct child and changes production
  only through four added lines in `services/api/build.zig`.
- Real outside-package RED — PASS. A clean `git archive cc03cc3` replay of
  `zig build test-build-discovery --summary all` fails exactly 20/21. The
  nested external package materializes the artifact, then Zig rejects the
  exact locked expression with `embed of file outside package path`.
- Exact-key mapping — PASS. The dependency key is byte-for-byte
  `../../../../tools/contracts/.generated/runtime/v1/route-inventory.json`,
  and its root is exactly
  `tools/contracts/.generated/runtime/v1/route-inventory.json` relative to the
  repository. No alias, wrapper, filesystem read, or second registry exists.
- Data-only least authority — PASS. The new dependency is rooted at JSON and
  exists solely to satisfy `@embedFile` bytes; it exposes no executable Zig
  declarations or persistence/migration capability. The only Zig module in
  `http_imports` remains `shared`.
- Materializer ordering — PASS. The exact repository-root `npm run
  contracts:generate` step still directly precedes the executable compile and
  every HTTP test compile. The new 1.2-second delayed fixture embeds and
  compares the exact generated bytes, so a stale or early compile fails.
- Current discovery — PASS, 21/21. The permanent exact-literal fixture,
  delayed materialization fixture, positive public graph, emitted API launch,
  and forbidden-persistence negative all execute through the production build
  graph.
- Least-authority regression — PASS. The HTTP module still receives no named
  `persistence` or `migrations`; the permanent negative continues to require
  `no module named 'persistence'`.
- Composition and executable behavior — PASS. Composition retains `shared`,
  `persistence`, `migrations`, and `http`; HTTP tests retain the full public
  graph plus `composition` and `http_test_config`; `getEmittedBin()`, direct
  materializer dependencies, API execution, and run argument forwarding are
  unchanged.
- Fail-closed behavior — PASS. Fresh absent-producer `test-http` and `run --
  forwarded-token` both fail nonzero with their stable owning-WP08
  diagnostics. The base build passes 3/3.
- Regression scope — PASS. Zig `0.16.0` formatting and diff checks pass. The
  adapter, ABI integration, vendored source, provenance, license, coverage
  runner, and instrumentation are unchanged, so the immediately preceding
  recorded cold-cache and ABI matrix remains applicable.
- Worktree hygiene — PASS. Review-created Zig cache/output was removed and the
  pre-existing `.spec-kitty/` state was preserved.

## Anti-pattern checklist

1. Dead code — PASS. The mapping is consumed by the exact locked `@embedFile`
   expression through executable and test compilation.
2. Synthetic-fixture test — PASS. The fixture executes the production build
   graph in an external package and compares bytes written by the delayed real
   materializer; the committed pre-fix graph demonstrably fails.
3. Silent empty return — N/A.
4. FR coverage — PASS for FR-015's independently runnable HTTP build boundary.
5. Frozen surface — PASS. Only WP04-owned build/discovery files changed.
6. Locked decision — PASS. The exact canonical path is read-only and no
   forbidden Zig capability, wrapper, registry, or generated write was added.
7. Shared-file ownership — PASS. `services/api/build.zig` remains WP04's
   authoritative build graph and the correction is explicitly coordinated for
   WP08 consumption.
8. Production fragility — N/A; no production exception or fallback path was
   added.

## Verdict

Approve WP04. Commits `cc03cc3` and `fd3f829` close the sole cycle-19 blocker
with a chronological external-package RED, a four-line data-only mapping, and
an exact permanent byte-level regression proof while preserving least
authority and every previously accepted HTTP build behavior.
