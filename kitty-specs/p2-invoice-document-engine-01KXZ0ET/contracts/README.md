# P2 Draft Contracts

`invoice-document-v1.schema.json` defines the structured Draft/Evaluated/
Snapshot vocabulary at the planning boundary. `p2-contract-manifest.json`
records Draft lifecycle and dependencies. Both are planning-only; implementation
and consumer reliance require Frozen artifacts and immutable fixture digests.

